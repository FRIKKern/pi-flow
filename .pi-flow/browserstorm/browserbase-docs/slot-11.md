# LangChain Python — Research (Browserstorm 11/20)

**Source:** [integrations/langchain/python.md](https://docs.browserbase.com/integrations/langchain/python.md)  
**pi-flow context:** LangChain + Stagehand

---

## Summary

Key points from documentation:

- `urls` Required. A list of URLs to fetch.
- `text_content` Retrieve only text content. Default is `False`.
- `api_key` Browserbase API key. Default is `BROWSERBASE_API_KEY` env variable.
- `session_id` Optional. Provide an existing Session ID.
- `proxy` Optional. Enable/Disable Proxies.

### Sections

**Loader options** — * `urls` Required. A list of URLs to fetch. * `text_content` Retrieve only text content. Default is `False`. * `api_key` Browserbase API key. Default is `BROWSERBASE_API_KEY` env variable. * `session_id` Optional. Provide an existing Session ID. * `proxy` Optional. Enable/Disable Proxies.

## pi-flow mapping

LangChain integration for Python pipelines; pi-flow is TypeScript/Pi-native.

---

## References

- [LangChain Python](https://docs.browserbase.com/integrations/langchain/python.md)
